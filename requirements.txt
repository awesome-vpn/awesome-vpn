# LEGACY — 本文件由 uv 管理，首选 `uv sync` (pyproject.toml + uv.lock)
# 保留此文件仅为兼容 `pip install -r requirements.txt` 及旧 CI
# 更新: `uv pip compile pyproject.toml -o requirements.txt` 或手动同步
# 主入口: pyproject.toml [project.dependencies] + uv.lock
requests[socks]>=2.32.0
PyYAML>=6.0.1
beautifulsoup4>=4.12.0
chardet>=5.2.0
cryptography>=42.0.0
paramiko>=3.4.0
scp>=0.15.0
geoip2>=4.7.0
maxminddb>=2.2.0
python-dotenv>=1.0.0
# NOTE: geoip2 pulls aiohttp; no extra pin needed.
